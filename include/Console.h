#pragma once
#include "Flight.h"
#include "Passenger.h"
#include "AVL_tree.h"
#include <cstring>
#include <iostream>
namespace Console {
    AVL_TREE manager;
    Flight *list;
    void book_ticket(Flight *booking) {
        

        // Ticket new_ticket;
 


        // Ticket new_ticket(new_seat, CMND);


        // if(!booking->valid_ticket_for_new_user(new_seat, CMND)) {
        //     return;
        // }



    }

    // void cancel_ticket() {
    //     char CMND[LEN_CMND];
    //     //nhập vào  
        
    //     Passenger &cur = manager.search(manager.get_root(), CMND)->data;
        

    //     //in ra flight_code để chọn vé xóa
    //     char flight_code[LEN_FLIGHT_CODE];
    //     //tìm vé có flight_code
    //     Ticket *x;

    //     strcpy(flight_code, cur.used)

    // }
}


